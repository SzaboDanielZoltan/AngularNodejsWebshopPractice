import { AdminOrderSortPipe } from './admin-order-sort.pipe';

describe('AdminOrderSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminOrderSortPipe();
    expect(pipe).toBeTruthy();
  });
});
