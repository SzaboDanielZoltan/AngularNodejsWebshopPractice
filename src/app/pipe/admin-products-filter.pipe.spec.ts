import { AdminProductsFilterPipe } from './admin-products-filter.pipe';

describe('AdminProductsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminProductsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
