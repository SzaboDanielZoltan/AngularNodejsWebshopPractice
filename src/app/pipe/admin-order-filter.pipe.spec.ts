import { AdminOrderFilterPipe } from './admin-order-filter.pipe';

describe('AdminOrderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminOrderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
