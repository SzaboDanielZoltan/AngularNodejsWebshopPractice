import { AdminProductsSortPipe } from './admin-products-sort.pipe';

describe('AdminProductsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminProductsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
